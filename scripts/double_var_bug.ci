 
// test BB_PUSH_RET — returns 10, 20, 30
var a, b, c = varrets_example()
print(a)
print(b)
print(c)

// test BB_RETURN — returns 100, 101, 102
var x, y, z = varrets_example()
print(x)
print(y)
print(z)
